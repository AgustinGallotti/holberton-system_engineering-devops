#!/bin/bash
touch school.mgc
function findTextInAsciiFiles {
    #usage: finTextInAsciiFiles 0x00-shell_basics NEEDLE_TEXT
    find "$1" -type f -exe grep -l "$2" {} \; -exec file {} \; | grep text
    }
